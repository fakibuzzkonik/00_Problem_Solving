#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,c,d,e,f,g,h,i,j,k;

    cin >> a;   //9
    a = sqrt(a);
    cout<< a <<endl;    //3

    cin >> b;
    b = fabs(b);    //negative value transfer in positive
    cout<< b << endl;

    cin >> a >> b;
    a = pow(a,b);   //a to the power b
    cout << a <<endl;

    ///ro onk math mathmatical function chilo likhi nai..
}
