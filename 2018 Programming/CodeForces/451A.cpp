#include<bits/stdc++.h>
using namespace std;
int main()
{
    int i,j,t,a,b,c,n,m,k,s;
    cin >> a >> b;
    n = min(a,b);
    if(n%2 == 0)
        cout<<"Malvika"<<endl;
    else
        cout<<"Akshat"<<endl;

    return 0;
}
